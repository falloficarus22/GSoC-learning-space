# Models

This folder contains the Mesa models I am building to understand the framework
from a modeller's perspective before proposing or updating PRs.

Current models:

- `cross_functional_teams/`: a small organization model that uses stable
  department groups and temporary project teams to explore overlapping
  memberships and meta-agent behavior.
